1. $sudo netstat -tulpn | grep --color :8080
check if 8080 is occupied by process, color matched keyword by --color.

2. $git reset --hard # removes staged and working directory changes
   $git clean -f -d # remove untracked files

Discard all changes, remove untracked files.

3. add -DLAPACK_LIBRARIES=%{_libdir}/liblapack.so.3 when compiling it++ if library auto detection fails.
   bug issue: http://sourceforge.net/p/itpp/bugs/231/
